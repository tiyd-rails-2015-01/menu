<p id="notice"><%= notice %></p>

<h1>Selections from the house</h1>

  <table>
  <thead>
    <tr>
      <th>Course name</th>
      <th>Item name</th>
      <th>Item Price</th>
      <th>Item Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.course.name %></td>
        <td><%= item.name %></td>
        <td><%= item.price %></td>
        <td><%= item.description %></td>
        <td><%= button_to 'Show', edit_item_path(item), :method => "get" %></td>
        <td><%= button_to 'Edit', edit_item_path(item), :method => "get" %></td>
        <td><%= button_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' }, :method => "get" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div>
<br>
  <%= button_to 'New item', new_item_path, :method => "get" %>
</br>
  <%= button_to 'Home', root_path, :method => "get" %>
</div>
  <div>
    <br>
      <h2>"Today's secret ingredient...LOVE!"</h2>
    </br>
  </div>
      <br>
      <!-- <%= image_tag "http://static.tvtropes.org/pmwiki/pub/images/ironchef.jpg" %> -->
      </br>
