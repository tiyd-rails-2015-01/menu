<body>
<p id="notice"><%= notice %></p>

<h1>Selections</h1>

<table>
  <thead>
    <tr>
      <th>Course name</th>
      <th>Item name</th>
      <th>Item Price</th>
      <th>Item Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.course.name %></td>
        <td><%= item.name %></td>
        <td><%= item.price %></td>
        <td><%= item.description %></td>
        <td><%= link_to 'Edit', edit_item_path(item) %></td>
        <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<br>
<%= link_to 'New item', new_item_path %>
</br>
</br>
<%= link_to 'Home', root_path %>
</body>
<div>
  <br>
  <%= image_tag "http://static.tvtropes.org/pmwiki/pub/images/ironchef.jpg" %>
</br>
</div>
